---
allowed-tools: Bash(npm run build:*), Bash(npm run lint:*)
description: Build the Next.js application for production
---

## Your task

Build the Next.js application for production:

1. First run lint check: `npm run lint`
2. If lint passes, run build: `npm run build`

Report any errors found during the process and suggest fixes.

**Common build issues:**

- TypeScript errors
- Missing dependencies
- Invalid imports
- Server/Client component mismatches
